export function SectionHeading({ title, href, action }: { title: string; href?: string; action?: string }) { return <div className="mb-5 flex items-end justify-between"><h2 className="text-xl font-bold tracking-tight text-white sm:text-2xl">{title}</h2>{href && <a href={href} className="text-sm font-medium text-amber-400 hover:text-amber-300">{action ?? "View all"} →</a>}</div>; }
export function EmptyState({ title, detail }: { title: string; detail: string }) { return <div className="rounded-xl border border-dashed border-zinc-700 bg-zinc-900/40 px-6 py-14 text-center"><h2 className="font-semibold text-zinc-200">{title}</h2><p className="mt-2 text-sm text-zinc-500">{detail}</p></div>; }
export function LoadingSkeleton({ count = 6 }: { count?: number }) { return <div className="grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-5 lg:grid-cols-6">{Array.from({ length: count }, (_, i) => <div key={i} className="aspect-[2/3] animate-pulse rounded-lg bg-zinc-800" />)}</div>; }
