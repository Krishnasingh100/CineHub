"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function AuthForm({ mode }: { mode: "login" | "register" }) { const router = useRouter(); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit(form: FormData) { setLoading(true); setError(""); const payload = Object.fromEntries(form); const res = await fetch(`/api/auth/${mode}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const data = await res.json() as { error?: string }; setLoading(false); if (!res.ok) return setError(data.error ?? "Could not continue."); router.push("/"); router.refresh(); } return <form action={submit} className="space-y-4">{mode === "register" && <input name="name" required maxLength={60} placeholder="Your name" className="w-full rounded-lg border bg-zinc-900 px-4 py-3 outline-none focus:border-amber-500"/>}<input name="email" type="email" required placeholder="Email address" className="w-full rounded-lg border bg-zinc-900 px-4 py-3 outline-none focus:border-amber-500"/><input name="password" type="password" required minLength={8} placeholder="Password (8+ characters)" className="w-full rounded-lg border bg-zinc-900 px-4 py-3 outline-none focus:border-amber-500"/>{error && <p className="text-sm text-red-400">{error}</p>}<button disabled={loading} className="w-full rounded-lg bg-amber-400 py-3 font-bold text-black disabled:opacity-60">{loading ? "Please wait…" : mode === "login" ? "Log in" : "Create account"}</button></form>; }
