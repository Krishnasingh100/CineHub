# Vercel -> Settings -> Environment Variables
# Local dev uses Vite proxy (/api -> localhost:5000), so this can stay empty locally.
# In production set it to your Render URL, e.g. https://cinehub-api.onrender.com
VITE_API_URL=https://cinehub-api.onrender.com
