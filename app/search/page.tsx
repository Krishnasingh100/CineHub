import { MovieGrid } from "@/components/movie-grid";
import { EmptyState, SectionHeading } from "@/components/ui";
import { searchMovies } from "@/lib/tmdb";
export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string | string[] }> }) { const { q = "" } = await searchParams; const query = typeof q === "string" ? q.trim() : ""; const results = query ? await searchMovies(query) : null; return <div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><form className="mb-10 flex max-w-2xl gap-3"><input name="q" defaultValue={query} placeholder="Search for a movie" className="min-w-0 flex-1 rounded-lg border bg-zinc-900 px-4 py-3 outline-none focus:border-amber-500"/><button className="rounded-lg bg-amber-400 px-5 font-bold text-black">Search</button></form>{results ? <><SectionHeading title={`Results for “${query}”`} /><MovieGrid movies={results.results}/></> : <EmptyState title="Find your next watch" detail="Search by movie title to explore the CineHub catalogue."/>}</div>; }
